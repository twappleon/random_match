name: random_match_mobile
description: Random video and voice matching app.
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.5.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  characters: ^1.4.0
  firebase_analytics: ^11.3.6
  firebase_core: ^3.9.0
  dio: ^5.7.0
  flutter_webrtc: ^1.4.1
  get: ^4.6.6
  in_app_purchase: ^3.2.3
  web_socket_channel: ^3.0.1
  firebase_messaging: ^15.2.10
  permission_handler: ^12.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
