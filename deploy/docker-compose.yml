services:
  mongo:
    image: mongo:7
    container_name: random-match-mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  redis:
    image: redis:7-alpine
    container_name: random-match-redis
    ports:
      - "6379:6379"

volumes:
  mongo_data:
